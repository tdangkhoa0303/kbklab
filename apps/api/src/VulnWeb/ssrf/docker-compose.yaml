version: '3.3'

services: 
  app_ssrf:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 3000
    image: app_ssrf_1
    stdin_open: true
    tty: true
volumes: 
  db-config:
  db-data:
